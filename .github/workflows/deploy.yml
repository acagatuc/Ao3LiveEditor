name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: |
            Ao3-Previewer/frontend/package-lock.json
            Ao3-Previewer/infrastructure/package-lock.json
            Ao3-Previewer/infrastructure/functions/package-lock.json

      - name: Install frontend dependencies
        working-directory: Ao3-Previewer/frontend
        run: npm ci

      - name: Build React app
        working-directory: Ao3-Previewer/frontend
        run: npm run build
        env:
          VITE_SENTRY_DSN: ${{ secrets.VITE_SENTRY_DSN }}
          VITE_API_URL: ${{ secrets.VITE_API_URL }}
          VITE_GA_MEASUREMENT_ID: ${{ secrets.VITE_GA_MEASUREMENT_ID }}

      - name: Install infrastructure dependencies
        working-directory: Ao3-Previewer/infrastructure
        run: npm ci

      - name: Install Lambda function dependencies
        working-directory: Ao3-Previewer/infrastructure/functions
        run: npm ci

      - name: Typecheck Lambda functions
        working-directory: Ao3-Previewer/infrastructure/functions
        run: npm run typecheck

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Deploy stack
        working-directory: Ao3-Previewer/infrastructure
        run: npx cdk deploy --require-approval never
        env:
          CERTIFICATE_ARN: ${{ secrets.CERTIFICATE_ARN }}
          HOSTED_ZONE_ID: ${{ secrets.HOSTED_ZONE_ID }}
          DOMAIN_NAME: ${{ secrets.DOMAIN_NAME }}
          CDK_DEFAULT_ACCOUNT: ${{ secrets.CDK_DEFAULT_ACCOUNT }}
          CDK_DEFAULT_REGION: ${{ secrets.CDK_DEFAULT_REGION }}
          CONTACT_EMAIL: ${{ secrets.CONTACT_EMAIL }}
          SES_FROM_EMAIL: ${{ secrets.SES_FROM_EMAIL }}
